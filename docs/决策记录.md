# 决策记录

> 只记「拍板过的取舍」：每条含日期、决策内容、理由、被放弃的替代项、影响面。按时间倒序追加。

| 日期 | 模块 | 决策 | 理由 | 放弃的替代项 | 影响 |
|---|---|---|---|---|---|
| 2026-07-03 | 00 | **Python 版本固定 3.14**（payipa `requires-python = ">=3.14"`，生产普通 GIL 构建，不启用 free-threaded） | 21 个关键依赖核验全绿；bugfix 支持到 2027-10；PEP 649 生态迁移期已过 | 3.13（约 2026-10 起 security-only，生命周期反而差） | 全项目；若用 Playwright 需开工第一周做双平台冒烟；并行代码按 spawn 语义写（Linux 默认 forkserver） |
| 2026-07-03 | 00 | **payipa 与 jianbing_utils 两个独立目录（双仓）同时开发**；开发期 payipa 经 `[tool.uv.sources]` path + `editable = true` 引入 jb，jb 发布后删 source 条目切 PyPI 版本 | 工具库定位为开箱即用、服务多个项目，不与 payipa 绑定 | 同仓 uv workspace（会被 workspace 统一 requires-python 牵制） | 工程结构；jb 依赖必须全部来自公开 index，发布前 `uv build --no-sources` 验证 |
| 2026-07-03 | 00 | **jianbing_utils `requires-python = ">=3.11"`** | jb 会给别的项目用，下限要宽 | 跟随 payipa 用 >=3.14 | jb 代码须保持 3.11 语法兼容；CI 测试矩阵 3.11/3.12/3.13/3.14 |
| 2026-07-04 | 01 | **pyp-agent 放 payipa monorepo 内**（workspace 成员，独立发 PyPI 包 + Docker 镜像） | 与主控共享契约包、联调零成本 | 独立仓 | agent 依赖须刻意保持最小集 |
| 2026-07-04 | 10 | **浏览器插件采用「平台 UI 页 ↔ 扩展浏览器内直通」架构，v1 只做 Chromium 系（Chrome/Edge）** | 零服务器新增面、无配对流程；Firefox 不支持 externally_connectable | 插件直连服务器 API + SSE 回填；Firefox 同期支持 | 插件代码放 monorepo `extensions/`；逻辑全打包（商店远程代码禁令），平台↔插件只传数据 |
| 2026-07-04 | 01 | **大文件存储以 S3 兼容对象存储为主后端**（环境已有）；**jianbing_utils 需提供分片上传/下载工具类**（multipart、presigned、断点续传） | 环境已有 S3；直传模式已验证为业界标准 | 自建文件中转服务 | jbutils 新增 s3 工具模块 |
| 2026-07-04 | 01 | **主↔子节点互联 = agent 出站 WebSocket 持久连接**（注册仍走一次性 join token，一行命令接入） | 实时双向、单通道承载注册/心跳/任务/状态 | HTTP 长轮询（实时性弱）、gRPC（依赖重） | 需设计心跳（15-30s）+断线指数退避重连+反代长连超时配置；**WS 只传小消息，大数据一律走数据通道** |
| 2026-07-04 | 01 | **agent 职责 = 抓取 + 大对象直传存储 + 回传指针与结构化结果**；入库动作在主控侧，agent 零 DB 凭证、零 S3 密钥（presigned） | 数据面不过主控；密钥不出主控 | agent 全功能（下载解析入库）、回传全量数据 | DB 永久记录对象 key（非临时 URL），与 task/agent 关联可回溯 |
| 2026-07-04 | 01 | **未配置对象存储时兜底 = 主控端本地盘**，agent 经主控 HTTP 流式分块上传接口回传（不走 WS）；主控做磁盘水位检查与告警 | 数据必须集中可查可备份；节点易失 | 存子节点本地 | UI 提示建议配置 S3；storage 抽象双后端，agent 上传代码同一套 |
| 2026-07-04 | 01 | **规则获取 = 任务带 (rule_id, version, hash) 指针 + agent 内容寻址缓存 + 未命中拉取**；配套规则版本状态机（draft→试跑→发布）与**测试通道**（任务分 test/prod 通道 + 节点分组 + 测试结果隔离存放） | 任务恒小、可复现、天然灰度；测试不污染正式数据 | 任务内嵌全量规则、主控主动推送规则 | 缓存按 hash 不可变，无需失效通知；测试通道细节在模块文档展开 |
| 2026-07-04 | 07 | **任务模型基线**：v1 触发方式全要（手动/cron 周期/一次性定时/API，cron 定义存 PG 调度大表）；两层模型「站点任务→请求数据任务」，agent 解析结果回传主控分流（入库/生成新请求任务）；遍历广度/深度可配默认广度；状态机全集；重试任务级可配+全局默认；节点自动均衡+可配置权重 | 一次对齐任务域全貌，避免分期返工；分流留在主控与「agent 零 DB」一致 | 分期实现触发方式；单层任务模型；agent 侧分流 | TaskSpec/状态帧契约设计（payipa-contracts）；任务界面归 06 模块 |
| 2026-07-04 | 07 | **队列载体 = Redis 有序优先级队列（sorted set），未配置时降级进程内存队列；PG 任务表为唯一权威状态**（如 0=未开始/2=进行中/1=完成），崩溃丢队列由 PG 重建，断点续传/暂停继续同源 | 队列要快且可选装；状态不容丢；重建语义让两种后端行为一致 | 纯 PG 队列（Procrastinate 等，不引 Redis）；Redis 侧强持久化（Streams/AOF） | 设计纪律：队列永远可由 PG 重建（队列=可再生视图）；redis-py 8.x asyncio 已核验 3.14 就绪 |
| 2026-07-04 | 07 | **限流 = 按完整子域名各自独立配额，默认 10 请求/秒，UI 可改**（www.xxx.com / news.xxx.com / good.news.xxx.com 分别独立计） | 尽量可控地访问对方 API | 按注册域聚合配额；按并发数维度限 | 限流键=子域名；自动调频在此天花板下工作（算法待定，届时或叠加「代理出口」维度） |
| 2026-07-04 | 07 | **批次与取消语义**：批次 = 任务（定时或一次性）的一次完整执行轮；取消 = 立即清空队列中该批任务 + 在途请求优雅收尾、回传已抓部分 | 取消要立即生效，又不丢已抓成果 | 仅优雅取消（等在途批次全部跑完） | 队列需支持按批次删除；agent 取消帧语义进 payipa-contracts |
| 2026-07-04 | 07 | **并发底座 = anyio**（asyncio 事件循环 + trio 式结构化并发 API）；主控与 agent 两侧统一用 task group / cancel scope 组织并发与取消 | 「取消整棵任务树」的语义正是需求（取消任务立即清掉相关协程）；FastAPI/Starlette 底座即 anyio，零生态损失 | 真 trio 事件循环（uvicorn/niquests/websockets/redis-py 全无 trio 支持，须换 Hypercorn 等）；裸 asyncio Task 管理 | 全项目 async 代码统一 anyio 风格；agent 侧（01 模块）同样适用 |
| 2026-07-04 | 07 | **自动调频方案**（详见 07 §2.5 草案）：限速按 (代理出口 × 子域名) 维度、用户配额为天花板；慢启动（10–20% 起步）+ 健康窗口加性提速；429/503 乘性减半、Retry-After 优先；403/验证码判封禁走换出口不降速；响应时长双 EWMA 仅作弱信号；失败率超阈值熔断；**v1 先做「慢启动 AIMD + Retry-After + 封禁分流」三件套** | 业界横向调研（AWS SDK/Netflix/Envoy/Heritrix/Crawlee）共同骨架：吞吐调优与防护应对分成两套机制 | 纯响应时长调频（Scrapy 模式：不看状态码、经代理失真）；纯本机资源调频（Crawlee 模式：429 死亡螺旋） | 各阈值默认值实现期定；代理出口维度依赖代理池设计（后续模块） |
| 2026-07-04 | 07 | **时间调度 = APScheduler 3.x（仅到点触发）+ 自研分发循环**；cron 定义权威在 PG 调度大表（调度器启动全量加载 + 变更重载），不用 APScheduler jobstore 持久化 | 3.11.3 稳定活跃、支持 3.14；避免 jobstore 与 PG 大表双权威；分发/队列/状态语义要自己掌控 | APScheduler 4.0（仍 alpha、schema 不稳）；SAQ 整包采用（吞掉队列控制权）；纯自研 croniter 触发循环 | 依赖钉 `apscheduler>=3.11,<4`；自研循环覆盖分发节流、状态推进、失联回收 |
| 2026-07-04 | 07 | **下发流控 = 槽位信用制 + 按空闲槽位分发**：agent 无本地缓存队列，注册上报并发槽 N（机器规格智能默认 + UI 可改），主控仅向有空闲槽的 agent 下发（在途 ≤ N）、取空闲槽最多者（平手轮询）；检测 = 任务租约超时 + agent 协程硬超时（anyio 取消范围）+ 心跳在途对账 | 缓存会让任务卡死 agent 端；槽位制容量即权重，agent 变慢自动少派（背压自适应），稳态等价加权轮询、异常态更优 | agent 本地队列缓存；静态加权轮询（会派给正忙节点造成堆积） | 心跳/状态帧带在途清单与空闲槽数（payipa-contracts）；限流与调频统一放主控 |
| 2026-07-04 | 07 | **优先级 = 用户任务页三档（高/中/低，默认中）**；v1 单队列，score = (优先级, 入队时间)；TaskSpec 与队列键预留 `group` 字段，将来「多队列 = agent 集群分组、任务定向发集群」扩展 | 简单够用；与 01 节点分组同构，预留字段扩展成本极低 | v1 即做多队列/多集群 | 集群分组细节归 agent 部署管理（09 或 01 扩展）讨论 |
| 2026-07-04 | 07 | **失败判定支持用户自定义**：默认按状态码语义；可配「HTTP 200 + 响应内容特征 = 软失败」，软失败重试优先换代理出口；重试默认 3 次、指数退避+抖动，429/503 跟随调频器且 Retry-After 优先 | 部分站点错误页返 200，换出口常可恢复 | 仅按状态码判失败 | 判定规则表达形式归 02（规则/清洗）细化；与调频方案的封禁内容特征检测共用机制 |
| 2026-07-04 | 06 | **界面 = 服务端渲染（SSR）**：Jinja2 + HTMX 2.0.x + Tabulator 6.x + sse-starlette + PyJWT + SQLAdmin（内部管理后台）；取消独立 SPA 前端与 `frontend/` 目录，界面由 apps/server 的 `templates/`+`static/` 承载（htmx/tabulator vendor 静态文件）；**前端实时通道 = SSE** | 单进程内闭环、无 node 构建链、内网部署友好；htmx 2.x 官方承诺永久支持（htmx 4 处 alpha 无压力）；SSE 有 htmx 官方扩展、与 sse-starlette 天然配 | 独立 SPA（React/Vue + node 工具链 + openapi→TS 类型生成）；实时通道用 WebSocket | `frontend/` 目录取消；openapi→TS 生成取消（OpenAPI 留给 AI 与对外 Dataset API）；鉴权模型与页面清单待 06 后续轮；sse-starlette/SQLAdmin 的 3.14 冒烟挂 POC |
| 2026-07-04 | 00 | **payipa-contracts 新增监控/遥测 schema（monitor.py）**：节点状态快照、队列统计、批次进度、限流调频状态；监控接口本体在 apps/server；**前端与 AI 经 FastAPI 自动生成的 OpenAPI 共享形状**（前端 openapi.json→TS 代码生成，AI 直读 openapi.json），前端不 import Python 包 | 监控数据形状是跨端契约；OpenAPI 是 FastAPI 原生自带零成本，机器可读文档天然就绪 | 在 contracts 里放"接口"实现（违反 contracts 无 I/O 纪律）；前端手写类型 | contracts 模型 Field description 必须认真写（喂前端类型/Swagger/AI 三处）；实时推送通道（SSE vs WS）归 06；MCP 化归 08 |
| 2026-07-04 | 02 | **规则/版型模型**：数据源配置含分组、描述、优先级、分发集群、重试、超时、调频限流；来源=URL 列表或授权 API；规则配置双分支（人工手填 xpath/css/jsonpath/regex 或插件点选；AI 帮配归 08）；字段级清洗=工具箱组合+js/python 脚本（固定方法名）；**版型识别=URL 正则+正文特殊值正则**；字段类型=存储/新链/存储+新链（新链可挂 URL 归一化） | 配置一页闭环；版型机制同时服务多版型区分与软失败判定 | 站点级单一规则（无版型概念） | contracts rule.py 按此展开；插件对接 10、AI 对接 08；浏览器自动化采集专场另述 |
| 2026-07-04 | 02 | **解析+清洗在 agent（或沙箱）执行**：有沙箱服务则 agent 发沙箱执行取结果，否则本地执行；声明式 JSON 由内置解释器跑，脚本经**代码执行器**（本地/沙箱同一接口抽象）；**脚本签名管理员统一配置**（用户无感）；沙箱通信按批不按条 | 与 01「agent 回传结构化结果」一致；签名满足 01 信任边界 | 清洗集中主控执行 | 沙箱协议与批处理归 04；重解析=按 raw 路径拉取重跑（任务类型入 07 体系） |
| 2026-07-04 | 02 | **raw 存档**：数据源级开关；zstd（3.14 stdlib）压缩归档，默认档 POC 定（倾向 9~12）；**默认保留 7 天**按源可配；raw 仅网页/JSON，存 `桶/数据配置UUID/raw/{批次}/{URL指纹}.zst`；**多媒体单独存 `桶/数据配置UUID/files/文件唯一ID`**；容量估算=静态初值（~30KB/页）+滚动实测+接 01 水位告警 | 重解析与排查刚需；stdlib zstd 零依赖 | 不留 raw（无法重解析）；不压缩 | raw GC 与 01 artifact GC 合并设计 |
| 2026-07-04 | 02 | **数据存储=按数据分表的混合 schema**：每源一表=固定系统列（id/批次关联/**数据指纹唯一索引**/created_at/**state smallint 负数错误码**）+用户字段 JSONB+勾选「需索引」生成 STORED 生成列+B-tree；`ON CONFLICT` 幂等入库；错误码固定枚举进 contracts | PG11+ 加列免重写、JSONB 免 DDL、生成列补强类型索引，三者取长 | 每字段实体列（迁移重）；纯 JSONB（无强类型索引）；EAV | 分表命名/分区规则挂遗留（需求方明示最后定）；tinyint 勘误为 smallint（PG 无 tinyint） |
| 2026-07-04 | 02 | **去重两层**：URL 指纹=hash(方法+规范化URL+排序请求体)，查询参数排序、JSON 体键排序，**参数清理黑/白名单数据源级可配**（内置常见跟踪参数黑名单）；数据指纹=业务字段组合排序 md5+唯一索引；**不用布隆过滤器**（假阳性漏抓+精确结构已够；超大规模再评估布谷鸟过滤器） | 业界标准（Scrapy 同构）；随机参数无自动解、配置化是共识 | 布隆过滤器前置层 | 自动化任务指纹加交互脚本标识维度（专场再定） |
| 2026-07-04 | 02 | **失败与修复**：处理失败记 state 负数错误码；重回队列至最大失败次数；**错误数据查看页可重采/重解析/重入库**；**测试默认不入库，勾选强制入库**（补数修数，运营功能）；**软失败特征=包含字符串+正则**（选择器命中留扩展），数据源级为主、版型级可覆盖；**角色雏形：管理员/技术/运营** | 修复动作界面闭环；软失败复用版型正则机制 | 软失败仅硬编码状态码 | 角色喂 06 权限矩阵；与 07 §2.5-④ 封禁内容特征检测共用机制 |
| 2026-07-04 | 02 | **规则版本化复用 01 机制**（draft→testing→active+内容寻址 hash+agent 缓存）；版本号可查 log、还原配置、按版本测试；配置页「获取源码」测试走 test 通道（不入正式库） | 一套版本机制贯穿采集与清洗规则 | 独立的清洗规则版本体系 | — |
| 2026-07-04 | 03 | **查询模块定案**：以任务为中心的查询视图（任务页选数据能力→批次日志/完成度→查产出数据默认最新）；**按数据源单表查**、跨表只在组装；Tabulator remote 模式（AND/OR 组合筛选/多列排序/页码分页服务端译 SQL）；行详情竖向展示；**SQL 查询=单独权限+四件套**（子查询包装分页/只读角色/语句超时/行数封顶+审计）；**分页=UI offset+深度上限，导出与对外 API keyset 流式**；导出 csv/excel/json/parquet 走异步；深度分析经 API 下沉下游 | 单表查范围纯粹；offset 深页 O(n)（10M 行深页 8s）vs keyset 恒定 ~1ms；SQL 开放必须多层守护 | 跨源联查在查询层；纯 keyset UI（不能跳页）；DuckDB 分析层（v1 不做） | 数据集/对外 API 挂组装产物页（04/05）；JSONB 筛排性能阈值 POC 定 |
| 2026-07-04 | 07/03 | **任务关联三层**：①数据能力**参数化**（技术定义运行参数：名称/类型/默认值/**上限**；运营建任务表单填参）；②**链式模板**（技术在能力 B 声明可承接 A 输出的字段映射；运营选 A 后勾「追加流程」填 N）；③**不做自由 DAG 画布** | 参数化消解能力复制（10页/15页问题）；链式模板让运营只见开关+数字；执行层复用 07 批次触发+血缘 | 每个组合预制新能力（仍保留为可选傻瓜入口）；n8n 式编排（对运营过重） | TaskSpec 扩展参数 schema/链式触发/血缘键（contracts+07 回填） |
| 2026-07-04 | 06/03 | **用户生命周期与审计**：账号**停用不删**（软删）；管理员**交接向导**批量转移资源 owner（可按业务分组）；**名下有资源不可删**（前置校验）；停用用户周期任务**默认暂停并告警**；**audit_log 操作日志**（任务操作/规则发布/强制入库/SQL 查询/权限变更/交接，保留期可配），事件形状落 contracts event.py | 离职交接硬需求；无主任务不能静默跑 | 物理删除账号；无审计 | 权限矩阵（角色×资源×动作）06 细化 |
| 2026-07-04 | 02/03 | **时间处理**：系统时间 created_at/updated_at 一律 **timestamptz（内部即 UTC 瞬时）**，展示按东八（会话时区/AT TIME ZONE/模板过滤器）；**业务时间双存**=原文 str 保真 + dateparser 解析 timestamptz（可空，失败留空原串仍在）；无时区信息按**数据源级默认时区**（默认东八）解析 | PG 官方 wiki 反对无时区 timestamp；双存使转换可逆（需求方「怕转不回来」成立）；dateparser 1.4.1 爬虫出身（Zyte）、200+ locale、中文相对时间、含 3.14 | 只存转换值（不可逆）；只存 str（无法排序筛选） | dateparser 进 02 清洗工具箱；02 系统列补 updated_at |
| 2026-07-04 | 04 | **组装（简单版 ETL）基线**：数据任务批次完毕**自动触发**（任务配「关联组装任务 id」，日志记触发/完成）；脚本环境预装 **pandas+Polars**；**产物=业务库新表**（沿用 02 混合 schema）；**产物查看复用 03 查询组件**（入口与外挂不同：数据集描述+对外 API 发布）；全量/增量按业务配置——**读侧水位+写侧指纹幂等**；表结构**加法式演进**（JSONB 零 DDL+勾索引 ADD COLUMN+破坏性变更拦截，Delta/dbt 同款原则） | 复用最大化；水位+幂等≈恰好一次；加法演进是 ETL 业界共识 | 全新查看页；仅指纹无水位；自动执行破坏性变更 | 固定方法名清单与 02 统一设计；产物分表命名并入 02 分表规则最后定 |
| 2026-07-04 | 04 | **数据供给=自研 Query Gateway**（需求方提出，研判通过）：结构化 table API（表/列/过滤/keyset，**不收 SQL 字符串**）+ **job_token JWT**（按次签发，scope=可读表清单+行数配额+租约时长有效期）+ **Arrow IPC 流**返回；沙箱网络出口仅放行 gateway、**零 DB 凭证**；**写路径对称 agent 模式**：沙箱产 parquet→presigned 直传对象存储→回指针→主控校验装载入业务库 | 结构化 API 无注入面；密钥不出主控、入库集中主控（与 01 同构） | 裸 SQL 通道；主控预取推送（不够灵活）；gateway 直写库 | gateway 端点在 apps/server（v1 同进程）；API 形状进 contracts；泛用件（Arrow 流/S3 分片）走 jbutils，业务胶水在 payipa |
| 2026-07-04 | 04 | **组装版本控制与可重算**：复用 01/02 版本机制（内容寻址+状态机+还原+test 试跑不入正式表）；**可重算三件套**=脚本版本 pin+运行记录输入范围（水位区间/批次清单）+写侧幂等；**资源限额三层**=容器 cgroup（内存/CPU）+任务级（租约超时+token 行数配额+产物大小）+worker 池并发 | 全部由既有机制组合，零新造 | 独立的组装版本体系 | 沙箱本体（威胁等级/部署/协议/js 运行时）在 04A 专章推进 |
| 2026-07-05 | 05 | **推送 = 推送组件模型**：出口仅两种——对外 Dataset API + HTTP 推下游；推送逻辑用 Python 写好**发布成组件**（如"推送 Ghost 博客"），复用 02/04 脚本基建（固定方法名/管理员签名/代码执行器/01 版本机制）；**三种触发**：手动单条（组装产物详情页按钮）/ 链路自动（批次组装完毕推增量，水位复用）/ 定时（内容由代码定，可推系统状态如机器情况→lark 群） | 组件化写一次处处挂；三触发覆盖单条修补/流水线/周报场景 | 内置固定目标类型清单（DB 直写/MQ/SFTP——组件模型天然可扩展，不预制） | 组件出站网络策略挂 04A；目标凭证=组件级配置+加密存储（默认记法待复核） |
| 2026-07-05 | 05 | **批次推送 + 指纹下游去重**：按批次推，平台随数据附**数据指纹 id**，去重责任在下游（平台不记下游已收状态）；失败重试复用 07（次数+指数退避）、连续失败死信+告警；**推送 = 07 任务体系的一种任务类型**；推送记录（范围/条数/成败/下游响应/耗时）入审计、告警通道可配 | 幂等边界清晰、平台侧无状态；零新造调度设施 | 平台记忆下游已收内容（状态重） | — |
| 2026-07-05 | 08 | **AI 能力基线**：管理员配置若干模型（凭证管理员级）；用户在任务界面选择模型写代码；**系统提示词项目默认提供、管理员可修改**；AI 生成受约束——按平台固定方法/参数标准、只引用**库白名单**；**新增库 = 管理员构建新 docker 镜像分支**，沙箱方可执行 | AI 输出必须落在可执行可校验的轨道内；生成脚本与人写脚本同轨（test 试跑→签名发布），不开新口子 | 用户自带模型 key；AI 自由生成任意代码 | 模型接入抽象候选 PydanticAI（00 已核验）；MCP 化仍归 08 遗留 |
| 2026-07-05 | 09 | **权限 = RBAC**：一用户可授**多种角色 + 特殊权限**，管理员可配置；新增**运维角色**——日志查看页仅运维权限可见（角色集合：管理员/技术/运营/运维） | 多角色+点状授权覆盖交接/兼岗 | 固定单角色制 | 权限矩阵 UI 归 06 收口；09 其余（部署/备份/可观测/升级/多集群）继续 |
| 2026-07-05 | 04A | **沙箱定位与拓扑**：威胁模型 = **防事故不防恶意**→ 隔离档位 v1 = **容器 + cgroup + 常驻 worker 进程池**（nsjail/gVisor 可选加固，Firecracker 不做）；**拓扑 = 就近 sidecar 不做中心沙箱**——agent 同机 sidecar（localhost、数据不出机），主控旁执行器跑组装/推送，无 sidecar 则本地执行降级；**声明式规则不进沙箱**（内置解释器本地跑）；脚本按批 + 进程池免冷启动；推送组件在主控侧执行器跑、出站按组件目标 | 中心沙箱 = 数据绕路 + 单点 + 链路风险（需求方担忧成立）；声明式占大头不进沙箱使性能问题消解 | 中心沙箱集群；对声明式规则也走沙箱 | js 运行时待定（v1 是否仅 python）；worker 池默认值实现期 |
| 2026-07-05 | 10 | **插件开发阶段不上架商店**：unpacked 开发者模式自加载调试；是否上架（Chrome Web Store/Edge Add-ons）产品化阶段再定 | 开发期免审核周期 | 开发期即上架 | 上架前复查商店政策（远程代码禁令架构已遵守） |
| 2026-07-05 | 04A/02/08 | **脚本仅 Python，去掉 js**；AI 写的抓取逻辑脚本同样进沙箱——脚本量增大由拓扑消化（agent 侧 sidecar 随 agent 机器横向扩展，无中心瓶颈） | 少一个运行时的选型/镜像/SDK 双份成本；「脚本很多」已在 sidecar 设计承载内 | js/python 双语（02 原案） | 02/08 已加修订注记；清洗工具箱与固定方法 SDK 仅出 python 版 |
| 2026-07-05 | 09 | **部署拓扑 v1**：主控 1 机（server+Redis）+ PG 1 机（**三库：`pyp` 平台 / `data_center` 采集数据 / `business` 组装产物**，库名可配）+ agent 机若干——**一机一 agent**（agent 出站连接不监听端口；并发靠槽位 N 而非多进程）+ 同机 sidecar 沙箱；**主控侧组装/推送沙箱可单独 1 机**（容器=worker 池、一个起步、可加容器/机器；主控配置**沙箱端点列表**按空闲派发）；对象存储用环境既有 S3；**agent 配置统一管理界面**（节点管理页） | 一机多 agent 无收益（N 已是进程内并发）且调度视图碎片化；组装数据本经 gateway 过网、沙箱位置不改数据路径 | 一机 10 agent×2=20 节点（口述原设想）；沙箱出站注册（v1 不需要） | 节点管理页归 06；按代理出口隔离的多 agent 场景出现再评估 |
| 2026-07-05 | 09 | **日志/可观测/备份**：日志存 PG 平台库 `pyp`，**默认保留 30 天、管理员可配**（超期定时清理）；基础系统观测=自带前端监控页（monitor schema+SSE 既定机制，不上 Prometheus）；**AI 调用观测=外接自部署 Langfuse**（SDK 4.13，MIT）；**备份 v1 不做平台内功能**（需求方拍板靠 PG 自身；文档注记风险+平台外 pg_dump 建议页） | 自带监控页零外部件；AI 观测交给专业工具；备份属环境职责 | Prometheus/Grafana；平台内建备份系统 | 升级策略草案（三层版本/Alembic/人工升级+测试组灰度）待需求方点头另记 |
| 2026-07-05 | 05 | **Dataset API = Query Gateway 对外版**（字段过滤+keyset 分页）+ **API key 鉴权**（可吊销/配额/按 key 限流）；AI 消费靠 OpenAPI（既定机制），MCP 化归 08；**用户级通知机器人**：每用户自配 lark 机器人（及邮件等内置轻量渠道），绑定到任务、选不同机器人通知不同任务进度/状态——与代码类推送组件分层并存 | 一套 gateway 两张脸（沙箱数据面+对外 API）；通知贴用户、零代码 | 独立实现对外查询栈；通知也做成代码组件（对运营过重） | API key 实现细节留实现期；内置通知渠道清单随需求加 |
| 2026-07-05 | 09/02/04 | **PG 三库划分**（纠正前一轮误记的「双库」）：**`pyp`**=平台库（全部系统元数据 + 操作日志）/ **`data_center`**=采集数据库（`data_*` 表）/ **`business`**=组装产物库；库名可配 | 采集数据与组装产物物理隔离、平台元数据独立 | 双库（前轮误记）；单库多 schema | 跨 database 不能 SQL join，本设计不需要；需联查再评估 fdw/schema |
| 2026-07-05 | 09 | **升级策略**：三层版本（平台 / 契约 version.py / agent）；平台升级用 **Alembic** 迁移（停服→迁移→起新版）；agent 握手检测过旧→节点页标记 + 给命令**人工升级**（不做远程下发）；灰度先测试节点组再生产 | 单实例短停机可接受；远程下发升级=新风险面 | 主控远程推送升级 | Alembic 版本实现期核验 |
| 2026-07-05 | 11 | **代理池 = `core.proxy` 模块（混合 IP / 代理中转）**：汇总隧道/长效/iplist 三类 provider（每商一 adapter）+ 混合池 + 单一直用；产出「代理配置」供 02 数据源规则**下拉选**；**溯源统计**（per-IP 次数/成功失败率、per-(出口×域) 成功率）喂 07 调频与 monitor；管理员配置、凭证加密 | 07 调频/02 换出口全依赖「出口」来源；混合多商提升可用性与溯源 | 只用单一隧道代理（现状）；代理逻辑散落各处 | **出口数据面定为 B 中转网关**（2026-07-05）；adapter 清单按采购接入；中转可独立部署 |
| 2026-07-05 | 09 | **监控 = `core.monitor` 模块**（**不在 core/agent——core 无 agent 模块**）：聚合 agent/机器/任务成功率失败率 + **数据质量（解析成功·失败·空白率）**；agent 仅回报原始计数、主控聚合；schema 在 contracts monitor.py + result.py，接口 apps/server，页 06 | 监控是主控侧聚合、跨 agent+任务+数据质量的全局视图 | 放 agent 侧（core 无 agent 模块、聚合需全局） | contracts 补数据质量字段 |
| 2026-07-05 | 01 | **agent HTTP 下载分层**：普通 = **niquests**；反检测 = **curl_cffi 0.15**（TLS/JA3/HTTP2 指纹，curl-impersonate，requests 兼容 + async，3.14 就绪）。三层反检测栈：curl_cffi(HTTP 指纹) → CloakBrowser(浏览器指纹) → 代理池出口(网络) | 指纹检测站点纯 niquests 绕不过；curl_cffi 是 Python 生态 TLS 指纹事实标准 | 只用 niquests；一律靠浏览器自动化（重） | 按站点难度选引擎；curl_cffi 进 agent 依赖 |
| 2026-07-05 | 11 | **代理池设计参考**（需求方给项目提炼）：中转固定入口（方案 B）有成熟先例——**ProxyCat**（Python/GPL，短效→固定入口、懒取、失效切换、多选路、HTTP+SOCKS5）、**malenia/ProxyCompose**（Java，多源二次组合+统一入口+计费溯源）、**majora**（供应链集群、内存级故障转移、多协议自动识别）；吸收懒获取/内存级故障转移/多协议/选路模式/计费溯源 | 中转固定入口是验证过的成熟模式，强化方案 B；ProxyCat 为 Python 最直接参考 | 从零设计 | 仍待定 B/A（11 §1.3-1）|
| 2026-07-05 | 02 | **API 型数据源认证**：需 key/token 的 API 源——**key 管理员配置**（加密存储）+ 数据模块**脚本封装一层**处理认证(header/token/签名)+翻页（复用固定方法 SDK + 代码执行器）；无认证 API 直配 URL | 各 API 认证异构，脚本封装最灵活；key 集中管理 | 认证配置硬编码到规则 | 与 05 推送凭证同思路（管理员统一、加密存储） |
| 2026-07-05 | 00 | **jianbing_utils 设计理念**：**自研**通用工具库——小而全、静态方法/纯函数、开箱即用、降低 API 学习成本；聚焦爬虫/数据方向（请求/解析/去重/管道）。借鉴业界优秀库的设计思路、**不复制第三方代码** | 明确 jbutils 定位与设计原则（避免版权风险） | 照抄第三方库代码 | 与「开箱即用工具库」定位一致；s3 分片为首个模块 |
| 2026-07-05 | 全局 | **纳入 7 项历史资料汲取点**：①FieldMeta 证据链+字段级降级(02)；②Deliver Outbox(05)；③沙箱 LLM Gateway(04A)；④源无关五形态 Connector 契约(01/02，v1 落网页+API、余预留)；⑤限流加 account/profile 维度(07/11)；⑥token 存 hash + 行级 owner/tenant 预留(03/02)；⑦架构红线汇总表 + 契约字段诚实标注(00 §3.8) | 取长补短：旧方案精华（clearpage FieldMeta / outbox / 五形态 connector / 分层凭证）补强新方案 | 忽略历史沉淀 | 各模块已加对应定案条；SDD 综合体现 |
| 2026-07-05 | 12 | **爬虫框架骨架 + 旧代码接入**：模板方法基类 `BaseCrawler` + 实现类 `SitemapCrawler`(USP 自动展开)/`ListDetailCrawler`/`ApiCrawler`/`BrowserCrawler`——框架管横切(生命周期/代理/限速/去重/FieldMeta/错误/契约)、子类补站点规则；**为 AI 继承补全、受基类契约约束**（test+签名同轨）；三档形态(声明式 spec/框架子类/裸脚本)同执行器。旧代码接入三路径：**HTTP_PROXY 环境变量零改造走中转**(B 红利)+`emit` SDK 产出接入+外部脚本 Connector 沙箱托管调度；scrapy 用 CrawlerProcess+平台 pipeline | AI 补规则需基类约束才不写飞；旧爬虫最少改造复用平台运维、渐进迁移获全能力 | 纯声明式(不够灵活)/纯裸脚本(不够约束)/重写旧代码 | framework 归属(agent 内 vs 独立 payipa-crawler 包)待定；SitemapCrawler 用 ultimate-sitemap-parser |
| 2026-07-05 | 全局 | **SDD 落地约束收口（外部评审后回写模块文档）**：①执行器生产默认 Sandbox、Local 仅降级(04A)；②出网两类分离——沙箱脚本仅 Gateway、推送组件主控执行器按目标出网(04A/05)；③data_fingerprint 表内/源内唯一、不改组合键(02)；④artifacts 补 owner/tenant/expires(01)；⑤push_outbox 补 next_retry_at/lease_until/last_error/owner(05)；⑥跨库先写数据后置状态+幂等、不上重型对账(SDD)；⑦护城河 M0 建边界逐阶段增强(SDD)；⑧RLS v1 应用层 owner 过滤、预留不启用(03)；⑨外部依赖价格/许可证开工复核、ProxyCat GPL 传染注(01/11) | 权威文档内部收口，避免实现期分歧；辩证：③④不改键(按源分表已隔离)、⑥轻量一致性(规模内不上分布式事务/对账) | 评审建议的重型 outbox+对账、组合唯一键 | SDD 为准，模块文档已同步 |
| 2026-07-05 | 01/07 | **自动化 = agent 可选能力 + 分组分发 + 引擎抽象**：自动化非全 agent 必备，引擎 = Playwright 兼容 API（标准 Playwright 或 **CloakBrowser** 反检测 Chromium drop-in、wrapper MIT、27.7k★）；agent 注册上报能力；需自动化的任务经 **agent 分组** 发到有能力的集群；每会话绑 11 代理出口 | 不锁定框架；分组复用 07 group；CloakBrowser「自带代理」与代理池天然对接 | 固定用 Playwright；所有 agent 都装自动化 | 引擎最终选型有需求时定；CloakBrowser-Manager 多 profile 观望（alpha） |
| 2026-07-05 | 00 | **地基三定案 + 待复核确认**：ORM = **SQLAlchemy 2.0 async + asyncpg**（转正式）；模块边界工具 = **import-linter**；core 模块正式命名 crawl/explore/studio/deliver/storage **+ 新增 `ai` 模块**（PydanticAI 实现，08 落此）；①待复核项经「其他按你走」确认（软失败=字符串+正则、推送组件凭证=组件级加密存储、升级策略草案采纳） | 开工前地基须固化；AI 能力需代码落点 | psycopg3（备选）；tach；AI 放 core 外 | ai 依赖 PydanticAI 进 core；agent 不含 AI 依赖 |
